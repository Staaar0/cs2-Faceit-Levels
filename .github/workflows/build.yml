name: Build CS2FaceitLevels CSSharp

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build CSSharp plugin
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore CS2FaceitLevels.csproj

      - name: Publish
        run: dotnet publish CS2FaceitLevels.csproj -c Release -o publish

      - name: Create package zip
        run: |
          mkdir -p package/addons/counterstrikesharp/plugins/CS2FaceitLevels

          cp publish/CS2FaceitLevels.dll package/addons/counterstrikesharp/plugins/CS2FaceitLevels/CS2FaceitLevels.dll

          echo "Package files:"
          find package -type f | sort

          test -f package/addons/counterstrikesharp/plugins/CS2FaceitLevels/CS2FaceitLevels.dll
          test ! -f package/addons/counterstrikesharp/configs/plugins/CS2FaceitLevels/CS2FaceitLevels.json

          cd package
          zip -r ../CS2FaceitLevels.zip addons

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: CS2FaceitLevels
          path: CS2FaceitLevels.zip

      - name: Create GitHub Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="cssharp-build-${GITHUB_RUN_NUMBER}-${GITHUB_RUN_ATTEMPT}"

          cat > release-notes.md <<EOF_NOTES
          Automated CounterStrikeSharp build for CS2FaceitLevels.

          File:
          - CS2FaceitLevels.zip

          Notes:
          - This is one managed .NET plugin DLL, so separate Linux/Windows zips are not needed.
          - Release zip contains only the plugin DLL install path.
          - The config auto-generates on the server at addons/counterstrikesharp/configs/plugins/CS2FaceitLevels/CS2FaceitLevels.json.
          - A default config template is kept in the repo at config/CS2FaceitLevels.json.
          - Requires CounterStrikeSharp installed on the server.
          - Still requires the CS2FaceitLevels workshop addon through MultiAddonManager for the FACEIT images.

          GitHub also provides:
          - Source code (zip)
          - Source code (tar.gz)
          EOF_NOTES

          gh release create "$TAG" \
            CS2FaceitLevels.zip \
            --title "CS2FaceitLevels CSSharp Build #${GITHUB_RUN_NUMBER}" \
            --notes-file release-notes.md \
            --target "$GITHUB_SHA"
